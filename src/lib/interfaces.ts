/**
 * interface for defining the properties of the User type
 * @type {User}
 */
export interface User {
  _id: string;
  first_name: string;
  last_name: string;

  username: string;
  phone_number: string;
  password: string;
}

